<li>
  <div class="row">
    <div class="<%= user.id == message.user_id ? 'message-sent' : 'message-received' %>">
      <%= message.body %> <br>
      <small><%= l(message.created_at, format: :long) %></small>
    </div>
  </div>
</li>